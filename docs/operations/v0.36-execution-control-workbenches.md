# v0.36.0 — Execution Control Workbenches

Tanggal engineering closure: 27 Juli 2026  
Status: **LOCAL/LAN-UAT ENGINEERING CANDIDATE — BUKAN PRODUCTION GO-LIVE**

## Scope

Rilis ini menutup Wave B/C blueprint untuk capability execution 057–061:

- Inventory Reservation Workbench dengan trace pemilik, remaining quantity,
  expiry, filter/pagination, dan controlled release beralasan.
- Purchase Contract 360 dengan line commitment, pagu, sisa nilai, maker-checker,
  release history, dan controlled release ke purchase order.
- Capacity & WIP Control Tower dengan finite-capacity harian, overload,
  utilisasi, operation scheduling, progres, material/labor cost, dan WIP.
- CAPA & Calibration Workbench dengan lifecycle berurutan, evidence minimum,
  SoD penerbit–penutup, due-date monitoring, calibration history, dan
  fail-closed instrument eligibility.

## Defense in depth

Migration `064_execution_rls_concurrency.sql`:

- mengaktifkan RLS dan policy cabang pada seluruh tabel execution baru;
- menjadikan view reservation/capacity/WIP sebagai `security_invoker`;
- menambahkan optimistic `version` pada aggregate mutable;
- menambahkan NULL-safe unique business key untuk purchase-contract release.

Lapisan repository/route menambahkan:

- branch/legal scope fail-closed;
- idempotency key pada mutasi kritis;
- row/advisory lock untuk numbering, contract release, dan finite capacity;
- stale-version conflict dan replay detection;
- validasi input server-side serta audit trail.

Authorization matrix mencakup 274 handler. OpenAPI execution memakai API
version 1.1.

## Evidence terverifikasi

| Gate | Hasil |
|---|---|
| Database source of truth | Migration 001–064 checksum-valid |
| Rollback disposable | 64 up → 63 down → 63 re-up PASS |
| Automated regression | 326/326 PASS |
| Isolated UAT database | 326/326 PASS, database disposable dihapus otomatis |
| RLS/IDOR/replay/version regression | 4/4 Wave 12 PASS |
| Visual desktop/mobile | 42/42 PASS |
| Visual overflow/console/a11y control | body 0, main 0, console 0, unlabeled button 0 |
| Accessibility static audit | 18/18 PASS |
| Secret scan | 604 file, 0 finding |
| Dependency tree integrity | `npm ls --omit=dev` PASS; optional `pg-native` tidak dipakai |
| LAN load 10 user | 220 request, 0 gagal; read p95 26 ms, write p95 56 ms |
| LAN load 25 user | 550 request, 0 gagal; read p95 41 ms, write p95 50 ms |
| Backup/restore drill | backup terenkripsi valid; restore 200 tabel, migration 064 |
| Runtime database role | `mat_erp_app` non-superuser; runtime grants refreshed |
| Release artifact | v0.36.0, 390 file, migration 064, DLP/integrity finding 0 |
| Consolidated LOCAL predeploy | 12/12 PASS — LOCAL BUILD READY |

Predeploy juga membuktikan runtime health HTTP 200/DB up, 3 approval policy
aktif, reporting fresh, 0 expired job/access, 0 unscanned-clean file, final
assurance 5 PASS/0 warning/0 blocking, dan backup/restore masih fresh.

Live npm advisory lookup telah dijalankan setelah Owner memberi persetujuan
eksplisit untuk mengirim metadata dependency ke registry resmi: 0 vulnerability.

## Gate yang sengaja tetap terbuka

Engineering automation tidak boleh menandai bukti manusia sebagai selesai:

- `SEC-UAT-001` tetap `READY_FOR_RETEST`;
- UAT bisnis untuk 13 role dan exception workflow;
- training attendance dan competency evidence;
- enam rekonsiliasi Finance/Operations;
- actual RTO/RPO exercise dan copy offsite immutable;
- Owner sign-off pada release SHA/migration yang sama;
- aktivasi VPS, domain, TLS, monitoring, dan production cutover.

Tahap engineering berikutnya adalah sisa Wave B (field encryption/key rotation
dan retention lifecycle), lalu Wave D Finance enterprise. VPS tetap terakhir.
