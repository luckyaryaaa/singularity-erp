# MAT ERP V2 v0.30.0 — P0.5 Closure Evidence

Tanggal verifikasi: 22 Juli 2026  
Lingkungan: LOCAL / PostgreSQL 16 / loopback-only development

## Outcome

Scope P0.5 transaction correctness ditutup. Perubahan memusat pada invariant
yang bila gagal dapat menyebabkan akses lintas entitas, overwrite master data,
over-fulfilment, atau pelebaran emergency privilege.

## Control matrix

| Area | Control aktif | Bukti otomatis |
|---|---|---|
| Organization | legal-entity scope, parent validation, branch consistency, recursive-cycle guard | IDOR list/create/update dan cycle negative tests |
| Fulfilment | all-lines linkage, duplicate aggregation, advisory lock, lifecycle revalidation | over-quantity, partial link, dan competing draft tests |
| Change Request | maker-checker, identifier allowlist, baseline check, row/advisory lock, unique pending, RLS | tampered payload dan stale approval tests |
| Emergency access | permission + exact data scope; missing context fail-closed | branch/project cross-scope negative tests |
| Authentication | login/MFA/password-change memakai grant database yang sama dengan runtime session | auth dan governance regression |
| Release | source version/migration dibandingkan ke manifest; SBOM wajib | release verifier dan predeploy step |

## Verification record

- Automated regression: **247 passed, 0 failed**.
- Authorization contract: **203 handler** tercakup oleh matriks route.
- Targeted P0.5 suite: **30 passed, 0 failed**.
- Database migration: `050_p0_5_transaction_correctness.sql` applied.
- Secrets tetap berasal dari `.env`; `.env` di-ignore dan tidak disalin ke paket.
- Release artifact + SBOM: **334 file scanned, 0 findings**; manifest menunjuk
  versi 0.30.0 dan migration 050.
- Predeploy LOCAL: **14/14 checks passed**; final assurance **5 pass, 0 warning,
  0 blocking**; backup berumur 7,0 jam dan dua restore drill tersedia.

## Release boundary

Closure ini menyatakan scope P0.5 selesai pada local build. Ia tidak menggantikan
LAN-UAT lintas fungsi, rekonsiliasi data cutover, Owner sign-off, production
key management, observability eksternal, restore drill pada target hosting,
atau keputusan go-live. Kesetaraan fitur penuh dengan SAP/Oracle/Dynamics 365
tidak diklaim; benchmark tersebut dipakai sebagai arah kontrol enterprise.
