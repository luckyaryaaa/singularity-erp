# systemd unit — /etc/systemd/system/mat-erp.service
# Aktifkan: systemctl daemon-reload && systemctl enable --now mat-erp

[Unit]
Description=MAT ERP V2 (Mandiri Abadi Teknik)
After=network-online.target postgresql.service
Wants=network-online.target
Requires=postgresql.service

[Service]
Type=simple
User=materp
Group=materp
WorkingDirectory=/opt/mat-erp/current
# Seluruh secret hanya di file env (root:materp, chmod 640).
EnvironmentFile=/etc/mat-erp/mat-erp.env
ExecStart=/usr/bin/node server.js
Restart=always
RestartSec=3
# Log terstruktur JSON ke journald; rotasi diatur journald (SystemMaxUse).
StandardOutput=journal
StandardError=journal
SyslogIdentifier=mat-erp

# Pengerasan systemd
NoNewPrivileges=true
ProtectSystem=strict
ProtectHome=true
PrivateTmp=true
PrivateDevices=true
ProtectKernelTunables=true
ProtectControlGroups=true
RestrictSUIDSGID=true
RestrictNamespaces=true
LockPersonality=true
MemoryDenyWriteExecute=true
CapabilityBoundingSet=
AmbientCapabilities=
SystemCallFilter=@system-service
# Folder state persisten dibagi antarrilis melalui symlink current/storage.
ReadWritePaths=/var/lib/mat-erp

# Batas sumber daya wajar untuk VPS kecil
MemoryMax=1G
TasksMax=256

[Install]
WantedBy=multi-user.target
